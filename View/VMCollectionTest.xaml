<Window x:Class="Wpf_FoodManager.View.VMCollectionTest"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Wpf_FoodManager.ViewModel"
        xmlns:local="clr-namespace:Wpf_FoodManager.View"
        mc:Ignorable="d"
        Title="VMCollectionTest" Height="450" Width="800">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="50" />
        </Grid.RowDefinitions>
        
        <ListView ItemsSource="{Binding FoodViewModels}" Margin="10">
            <ListView.ItemTemplate>
                <DataTemplate DataType="{x:Type vm:FoodViewModel}">
                    <Border BorderBrush="Azure" BorderThickness="2" >
                        <StackPanel >
                            <TextBlock Text="{Binding ID.Value}" />
                            <TextBlock Text="{Binding Name.Value}"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <Button Grid.Row="1" Content="追加!!!" Click="Button_Click" />
    </Grid>
</Window>
